状态机-可重入-控制块-需要结构体，有多少个这样的状态机理论上就需要多少控制块

void *是要尽可能避免使用的——只有当要指向的目标类型是不确定的，而且没有别的解决方案的时候才用void *，
如果要指向的类型是确定的，而且不会改变的，比如指向队列，肯定直接用目标类型

为了保证状态机在任何不同的翻译情况下都能拥有正确的行为，局部变量只能在具体的
状态内部使用，生命周期不应该超过当前状态——即不存在跨状态的局部变量。